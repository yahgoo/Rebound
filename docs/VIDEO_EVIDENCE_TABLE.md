# Rebound — Video Evidence Table

**Source of truth for every badge/claim in every video cut.**
Generated 17 Aug 2026 from code state at commit `b9d89a1` + live Daytona capture.

| Capability | Status | Evidence source | Caveat text for narration/badge |
|---|---|---|---|
| **EXECUTOR active path** | REAL | `ops/demo.sh:40` `EXECUTOR="${EXECUTOR:-local}"`; runs in-process `LocalExecutor` at `packages/executors/local.py` | "Scoring runs locally in the demo for speed; the same code path runs in isolated sandboxes when EXECUTOR=daytona." |
| **Daytona sandbox provisioning** | REAL | `packages/executors/daytona.py`; verified 17 Aug: 8 sandboxes provisioned in 1.5-23s, total 26s, PARITY OK, zero survivors. Full capture: `output/daytona-capture-17aug2026.txt` | "Daytona provisions disposable sandboxes in seconds (1.5-2s typical). Code executes with no network access and is cleaned up immediately after." |
| **Telegram family notification** | ILLUSTRATIVE | `.env` — no `TELEGRAM_BOT_TOKEN` or `TELEGRAM_FAMILY_CHAT_ID` configured; `packages/agents/caretaker.py:97,223` reads from env; `telegram_sent=false` in all receipts | "Family notification via Telegram is implemented but not connected to an active bot in this sandbox demo. It will be configured with the operator's own Telegram credentials in deployment." |
| **A9 parity evidence scope** | REAL (scoped) | Two independent fresh-identity runs (run-5 on `TESTA20260816190635232`, run-6 on `TESTA20260816191909855`): both PARITY OK with identical live/replay candidates, recommendation, receipt. Evidence: `docs/DUPLICATE_BOOKING_TASKS.md` §18-19 | "Live/replay parity was verified across two independent runs on fresh identities, each showing identical agent steps and recovery outcomes. Parity depends on deterministic replay via the cassette-key fix (A8) and search-dedup fix (A9) — not a general guarantee across all routes and passenger types." |
| **604/decline chaos trigger reliability** | NOT-VERIFIED | `packages/atlas/chaos.py:107-125` — Atlas sandbox ignores cardholder name "Reject"/"Three DS" triggers; no genuine 604/616 cassette exists in any of 282 cassette files; `CHAOS_PROFILE=timeout` demonstrates failover via real transport timeout | "The sandbox payment-decline trigger does not produce a genuine 604 response from Atlas. Rebound's failover path is verified via timeout-driven failover instead, which exercises the identical code branch. A real 604/616 from a live payment gateway would follow the same path." |
| **Gemini model interpretation** | REAL | `.env`: `GEMINI_VIA=openrouter`, `OPENROUTER_API_KEY` set; `packages/agents/interpreter.py` calls model for constraint extraction; `warm_model` verified 1.58s ping in prior runs | "Gemini (google/gemini-3.6-flash via OpenRouter) interprets traveller intent into structured constraints. Model writes scoring code and copy templates — never invents flight numbers, prices, or receipt figures." |
| **Gemma sovereign model path** | NOT-VERIFIED | `apps/api/settings.py:69` `gemma_endpoint: str \| None = None` declared, **unused**; `docs/NOSANA_TASKS.md` outlines integration but no code wired | "Self-hosted Gemma on Nosana infrastructure is explored for sensitive booking data that must not reach a third-party model provider. Not yet integrated into the agent pipeline." |
| **Cassette replay — FAMILY identity** | REAL | `TESTA20260815002134580` — clean success chain with A8 salted-hash keys, PARITY OK achieved (A8 run-2, A9 run-4). Cassette count: 282 files in `fixtures/cassettes/` | "FAMILY identity has a clean end-to-end cassette chain verified for live/replay parity. Used as the default demo identity." |
| **Cassette replay — TAN identity** | PARTIALLY-BROKEN | `TESTA20260815020605810` — order.do cassette records auth failure (900). Identity fully exhausted per `fixtures/seeded_orders.json`. Default fallback changed to FAMILY (Fix 2, 17 Aug). | "TAN identity's default cassette records an authentication failure — a cosmetic fixture issue, not a live code defect. The demo was using an exhausted sandbox identity. Default identity changed to FAMILY." |
| **Cassette replay — BIZ identity** | USED (no clean chain) | `TESTA20260815002321968` — used in prior runs but no complete cassette chain for full replay (A6 replay failed with cassette_miss x2) | "BIZ identity was used in prior demo runs but does not have a complete cassette chain for end-to-end replay." |
| **Nosana / sovereign GPU** | ILLUSTRATIVE | `docs/NOSANA_TASKS.md` — architecture and integration plan documented; no `NOSANA_API_KEY` or `NOSANA_ENDPOINT` in `.env` or wired code | "Nosana is being explored for private, on-infrastructure model inference so sensitive booking details never reach a third-party provider. This is a design prototype, not a shipped capability." |
| **Operator console (browser)** | REAL | FastAPI + Jinja2 + HTMX SSE at `apps/web/templates/`; three-pane layout (case, trace, traveller); requires bearer token | "The operator console is live and functional. It requires a bearer token — demonstrated as a security boundary, not a bug." |
| **Recovery Receipt with event IDs** | REAL | `packages/agents/caretaker.py` — counterfactual math, attempt listing, `event_ids_json` stored; `RECEIPT` stage runs in demo | "The recovery receipt is computed from real Atlas data and event logs — fully auditable, no AI-generated figures." |
| **Guardian spend cap** | REAL | `packages/guardian/` — `MAX_SPEND_SGD` enforced at verification; verified with `GUARDIAN_MAX_SPEND_SGD=1` causing 409 | "A hard spend ceiling is enforced before any booking attempt, not after. The model cannot override it." |